<UserControl x:Class="POSSEDQI.Views.InvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Background="#0f172a" >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- شريط البحث -->
            <RowDefinition Height="*"/>
            <!-- المحتوى الرئيسي -->
            <RowDefinition Height="Auto"/>
            <!-- الأزرار -->
        </Grid.RowDefinitions>

        <!-- ✅ شريط البحث -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,15" HorizontalAlignment="Left" VerticalAlignment="Top">
            <!-- مربع البحث -->
            <TextBox Name="SearchBox"
                     Width="200" Height="30" Margin="0,0,100,0"
                     VerticalContentAlignment="Center"
                     Text="ابحث برقم الفاتورة" HorizontalContentAlignment="Right"
                     Foreground="Gray" FontSize="18"
                     GotFocus="SearchBox_GotFocus"
                     LostFocus="SearchBox_LostFocus"/>

            <!-- إلى التاريخ -->
            <DatePicker Name="StartDate" Width="140" Height="30" Margin="10,0,15,0" />
            <TextBlock Text="إلى التاريخ" VerticalAlignment="Center" Margin="0,0,10,0"
                       Foreground="White" FontSize="18" FontWeight="Black"/>

            <!-- من التاريخ -->
            <DatePicker Name="EndDate" Width="140" Height="30"  Margin="10,0,15,0"/>
            <TextBlock Text="من التاريخ" VerticalAlignment="Center" Margin="0,0,10,0"
                       Foreground="White" FontSize="18" FontWeight="Bold"/>
        </StackPanel>

        <!-- ✅ المحتوى الرئيسي (جدول الفواتير + تفاصيلها) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <!-- جدول الفواتير -->
                <ColumnDefinition Width="*" />
                <!-- تفاصيل الفاتورة -->
            </Grid.ColumnDefinitions>

            <!-- جدول الفواتير -->
            <DataGrid Grid.Column="0" Name="InvoiceDataGrid"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      Margin="0,0,15,0" 
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="All">

                <DataGrid.Columns >
                    <DataGridTextColumn Header="حالة الفاتورة" Binding="{Binding Status}" Width="*">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="الباقي" Binding="{Binding Remaining}" Width="*">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="المدفوع" Binding="{Binding PaidAmount}" Width="*">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="إجمالي الفاتورة" Binding="{Binding TotalAmount}" Width="*">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="اسم الزبون"  Binding="{Binding CustomerName}" Width="*">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="تاريخ الفاتورة" Binding="{Binding InvoiceDate}" Width="*">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="رقم الفاتورة"  Binding="{Binding InvoiceNumber}" Width="*">
                        <DataGridTextColumn.HeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="16"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.HeaderStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- تفاصيل الفاتورة -->
            <Border Grid.Column="1" Background="#f9f9f9" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="تفاصيل الفاتورة" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <!-- تفاصيل إضافية -->
                    <TextBlock Text="تفاصيل سيتم عرضها هنا..." FontStyle="Italic" Foreground="Gray"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- ✅ الأزرار -->
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,15,0,0">
            <Button Content="🖊 تعديل الحالة" Width="120" Height="32" Margin="0,0,10,0"/>
            <Button Width="100" Height="32" Margin="0,0,100,0" Background="Red" Foreground="White" ToolTip="حذف الفاتورة من جدول الفواتير">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="حذف الفاتورة" VerticalAlignment="Center" FontSize="18" Margin="0,0,10,0" FontWeight="Black"/>
                    <Image Source="/Icons/delete.png" Width="20" Height="20" />
                </StackPanel>
            </Button>
            <Button Content="🖨 طباعة الفاتورة" Width="140" Height="32" Margin="0,0,10,0"/>
        </StackPanel>
    </Grid>
</UserControl>
