<UserControl x:Class="POSSEDQI.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#0f172a">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- البحث والأزرار -->
            <RowDefinition Height="*"/>
            <!-- المنتجات -->
        </Grid.RowDefinitions>

        <!-- شريط البحث والتحكم -->
        <StackPanel Orientation="Horizontal" Margin="0 0 0 5" VerticalAlignment="Top">
            <!-- مربع البحث -->
            <TextBox x:Name="SearchBox" Width="200" Height="32" Margin="0,0,10,0" Padding="5" VerticalContentAlignment="Center"
                     Text="... ابحث عن" HorizontalContentAlignment="Right" Foreground="Gray"
                     GotFocus="SearchBox_GotFocus" LostFocus="SearchBox_LostFocus" FontSize="14"/>

            <!-- فلترة حسب الفئة -->
            <ComboBox Width="200" Height="32" Margin="0,0,115,0" Padding="5" FontSize="16"
                      VerticalContentAlignment="Center" HorizontalContentAlignment="Right">
                <ComboBoxItem Content="كل الفئات" IsSelected="True" FontSize="16"/>
                <ComboBoxItem Content="فئة 1"/>
                <ComboBoxItem Content="فئة 2"/>
            </ComboBox>

            <!-- أزرار التحكم -->
            <Button Width="100" Height="32" Margin="0,0,100,0" Background="Green" Foreground="White" ToolTip="إضافة منتج إلى المخزون">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="إضافة " VerticalAlignment="Center" FontSize="18" Margin="0,0,10,0" FontWeight="Black"/>
                    <Image Source="/Icons/add.png" Width="20" Height="20" />
                </StackPanel>
            </Button>
            <Button Width="100" Height="32" Margin="0,0,100,0" Background="Red" Foreground="White" ToolTip="حذف منتج من المخزون">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="حذف" VerticalAlignment="Center" FontSize="18" Margin="0,0,10,0" FontWeight="Black"/>
                    <Image Source="/Icons/delete.png" Width="20" Height="20" />
                </StackPanel>
            </Button>
            <Button Width="90" Height="32" Margin="0,0,100,0" Background="Orange" Foreground="White">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="تعديل" VerticalAlignment="Center" FontSize="18" Margin="0,0,10,0" FontWeight="Black"/>
                    <Image Source="/Icons/edit.png" Width="20" Height="20" />
                </StackPanel>
            </Button>

            <Button Width="150" Height="32" Margin="0,0,10,0" Background="#0285c7" Foreground="White">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="طباعة الكتالوج" VerticalAlignment="Center" FontSize="18" Margin="0,0,10,0" FontWeight="Black"/>
                    <Image Source="/Icons/print.png" Width="20" Height="20" />
                </StackPanel>
            </Button>

        </StackPanel>

        <!-- عرض المنتجات على شكل بطاقات -->
        <Border Grid.Row="1" CornerRadius="10" Background="#162033" Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <WrapPanel ItemWidth="200" ItemHeight="250" Margin="0" Orientation="Horizontal">

                    <!-- بطاقة منتج -->
                    <Border Width="150" Height="220" Background="White" Margin="5" CornerRadius="10">
                        <StackPanel Margin="5">
                            <Image Source="/Icons/sample-product.jpg" Height="100" Stretch="Uniform"/>
                            <TextBlock Text="اسم المنتج" FontWeight="Bold" FontSize="14" TextAlignment="Center" Foreground="Black" Margin="0,10,0,0"/>
                            <TextBlock Text="الوصف المختصر" Foreground="Black" FontWeight="Bold" FontSize="14" TextAlignment="Center"/>
                            <TextBlock Text="الكمية: 10" Foreground="Black" Margin="0,5,0,0" FontWeight="Bold" FontSize="14" TextAlignment="Center"/>
                            <TextBlock Text="سعر الشراء: 50 درهم" Foreground="Black" FontWeight="Bold" FontSize="14" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <Border Width="150" Height="220" Background="White" Margin="5" CornerRadius="10">
                        <StackPanel Margin="5">
                            <Image Source="/Icons/sample-product.jpg" Height="100" Stretch="Uniform"/>
                            <TextBlock Text="اسم المنتج" FontWeight="Bold" FontSize="14" TextAlignment="Center" Foreground="Black" Margin="0,10,0,0"/>
                            <TextBlock Text="الوصف المختصر" Foreground="Black" FontWeight="Bold" FontSize="14" TextAlignment="Center"/>
                            <TextBlock Text="الكمية: 10" Foreground="Black" Margin="0,5,0,0" FontWeight="Bold" FontSize="14" TextAlignment="Center"/>
                            <TextBlock Text="سعر الشراء: 50 درهم" Foreground="Black" FontWeight="Bold" FontSize="14" TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <!-- سنبرمج دالة لإنتاج بطاقات المنتجات تلقائيا مرتبطة بزر إضافة المنتج -->

                    <!-- يمكنك تكرار البطاقة لمنتجات أخرى -->

                </WrapPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
